# Data

This directory should contain the following directory structures to reproduce the figures:

```bash
data/
    |
    raw/ # Raw data results from pipeline run
	|
	bulk/ 		# Bulk data results
	single-cell 	# Single cell data results
	multimodal	# Multimodal data results
    processed/	# Processed objects for plotting
```
